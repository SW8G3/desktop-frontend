# Requirements
- NodeJS


# Environment File
1. Create a `.env` file in the root directory.
2. Insert the following values:
```
VITE_API_URL="http://localhost:3001"
```

# Frontend setup
1. Make sure you have the admin-backend and docker running
2. `npm i`
3. `npm run dev`

